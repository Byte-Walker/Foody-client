import React from 'react';
import './PlaceOrder.css';

const PlaceOrder = () => {
    return (
        <div>
            <h1 className="section-title">Welcome to the place order page</h1>
        </div>
    );
};

export default PlaceOrder;
